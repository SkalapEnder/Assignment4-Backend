<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body{
            background-color: #dbdbdb;
        }
    </style>
</head>
<body>
<%- include('../templates/navbar'); %>

<div class="container bg-white shadow rounded w-50 my-4 text-center p-5 pb-4">
    <h1>Profile Page</h1>
    <div class="profile-image">
        <img src="https://eu.ui-avatars.com/api/?name=<%= user.username %>&size=150" class="rounded-circle" alt="Profile Picture" />
    </div>

    <div class="mt-4">
        <div class="mt-4">
            <div class="d-flex">
                <h3>ID: <span data-no-translate><%= user.user_id %></span></h3>
            </div>

            <div class="d-flex">
                <h3 class="me-2">Name: </h3> <h3><span data-no-translate><%= user.username %></span></h3>
            </div>

            <div class="d-flex">
                <h3 class="me-2">Email: </h3><h3><span data-no-translate><%= user.email %></span></h3>
            </div>

            <div class="d-flex">
                <h3 class="me-2">Role:</h3><h3> <span data-no-translate><%= capitalizeFirstLetter(user.role) %></span></h3>
            </div>
        </div>

        <div class="mt-4">
            <h3>Profile data info</h3>
            <h4>Created: <%= convertData(user.createdAt) %></h4>
            <h4>Updated: <%= convertData(user.updatedAt) %></h4>
        </div>
    </div>

    <div class="my-4 mx-auto">
        <% if (user.role === 'admin') { %>
            <form action="/admin" method="GET" style="display: inline;">
                <button type="submit" class="btn btn-outline-primary fs-5">Admin Page (Users)</button>
            </form>
            <form action="/admin-items" method="GET" style="display: inline;">
                <button type="submit" class="btn btn-outline-primary fs-5">Admin Page (Items)</button>
            </form>
        <% } %>
    </div>
    <div class="mt-4 mx-auto">
        <form action="/update" method="GET" style="display: inline;">
            <button type="submit" class="btn btn-outline-success fs-5">Update User</button>
        </form>
        <form action="/logout" method="GET" style="display: inline;">
            <button type="submit" class="btn btn-outline-secondary fs-5">Log Out</button>
        </form>
        <form action="/delete-account" method="GET" style="display: inline;">
            <button type="submit" class="btn btn-outline-danger fs-5">Delete Account</button>
        </form>
    </div>
</div>

<div class="py-2 text-center fs-5 fixed-bottom text-white bg-dark">
    <h4>Alisher Berik | IT-2308</h4>
</div>
</body>
</html>
