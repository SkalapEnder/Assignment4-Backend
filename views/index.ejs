<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body{
            background-color: #dbdbdb;
        }
    </style>
</head>
<body>
<%- include('./templates/navbar'); %>

<main class="container-fluid text-center mt-4 p-5 w-50 shadow bg-white rounded">
    <h1>Welcome to Your Dashboard!</h1>
    <p class="fs-4">Welcome to my assignment 4! There you can see News about Space.</p>
    <p class="fs-4">Also you can search interesting stars!</p>
    <a class="link-offset-2 link-underline link-underline-opacity-0 fs-4 fw-bold" href="/build">Go to search star</a>

    <h2 class="text-center">Featured Items</h2>

    <div class="row">
        <% items.forEach(item => { %>
            <div class="col-md-6 mb-4">
                <div class="card shadow">
                    <div class="card-body">
                        <h4 class="card-title"><%= item.name[lang] %></h4>
                        <p class="card-text"><%= item.description[lang] %></p>
                    </div>
                    <div id="carousel-<%= item._id %>" class="carousel slide" data-bs-ride="carousel">
                        <div class="carousel-inner">
                            <% item.images.forEach((img, index) => { %>
                                <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                    <img src="/<%= img %>" class="d-block w-100" alt="Item Image">
                                </div>
                            <% }); %>
                        </div>
                        <button class="carousel-control-prev" type="button" data-bs-target="#carousel-<%= item._id %>" data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        </button>
                        <button class="carousel-control-next" type="button" data-bs-target="#carousel-<%= item._id %>" data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        </button>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</main>

<%- include('./templates/footer'); %>
</body>
</html>
